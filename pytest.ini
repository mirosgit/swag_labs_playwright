[pytest]
testpaths = tests
addopts =
    -q
    --disable-warnings
    --maxfail=1
    --alluredir=allure-results
    -n auto
    --dist loadgroup
reruns = 2
reruns_delay = 2

markers =
    smoke: fast critical checks (smoke coverage)
    regression: extended regression testing
